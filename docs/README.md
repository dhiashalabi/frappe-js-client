# Docs site

Docusaurus app for [frappe-js-client](https://github.com/dhiashalabi/frappe-js-client).

From the repo root:

```bash
pnpm install
pnpm docs:start
pnpm docs:build
```

Do not run `pnpm install` only inside this folder. The package is a workspace member; install from the repository root.
